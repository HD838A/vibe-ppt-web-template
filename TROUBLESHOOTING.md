# 常见问题与发布前排查

这份清单记录把真实内容套入模板时最容易暴露的问题。所有规则均为通用经验，不依赖任何具体产品、人物或私有素材。

## 演讲模式顶部或底部有空白

**原因**：演讲模式仍强制保持 16:9，在 16:10 或其他比例的屏幕上产生 letterbox 留白。

**处理**：滚动模式保留 16:9；演讲模式让 `.deck`、`.slide-frame` 和 `.slide` 填满 `100vw × 100vh`。同时在非 16:9 视口检查四边间距，而不是只在 1280 × 720 下截图。

## 图片加载成功但关键内容被裁掉

**原因**：`cover` 会主动裁边，`center` 也未必是素材的真实视觉焦点。设备顶部按钮、手机状态栏和二维码边缘尤其容易被裁掉。

**处理**：产品图、界面截图、设备图和二维码优先使用 `contain`；需要裁切时明确设置 `imagePosition`，并逐页查看关键区域是否完整。

## 图片出现额外白底或白边

**原因**可能来自两层：素材文件自身已有白色像素或透明外边距；主题图片容器又添加了白色背景、边框或阴影。

**处理**：先检查原始像素和透明通道，再检查容器样式。`object-fit` 不能消除图片文件自身的白色画布。

## 一张图正常，多张图后布局拥挤

**原因**：单图版式被临时塞入多张同级图片，导致尺寸、说明文字和间距失去约束。

**处理**：为多图内容使用明确的数组字段和 Grid 布局，给每张图独立的 `alt`、说明与最大尺寸；在窄视口定义降级方式。

## 换主题后出现多余文案或错误素材

**原因**：主题层承担了内容逻辑，例如自动替换暗色截图、注入说明或改写名称。

**处理**：主题只负责字体、颜色、布局、边框、阴影和动效。内容差异必须由内容文件显式声明，不能由 CSS 或主题 ID 推断。

## 鼠标能翻页，键盘上下层级不完整

**原因**：只实现了点击或左右键，没有统一映射 `PageUp`、`PageDown`、空格、`Home`、`End`、上下方向键和 `Escape`。

**处理**：先定义页面的导航层级，再让鼠标按钮和键盘共享同一状态更新逻辑。每次交互后检查活动页数量始终为一。

## 动画存在，但页面切换显得突兀

**原因**：非活动页直接使用 `display: none`，上一页无法完成淡出；或所有主题共用同一运动节奏。

**处理**：用 `opacity`、`visibility` 和主题 motion token 完成交叉切换；同时支持 `prefers-reduced-motion`。

## 自动检查通过，实际页面仍有问题

**原因**：构建成功和图片 `naturalWidth > 0` 只能证明资源可用，不能证明文字不重叠、关键区域未裁切、素材内文字正确。

**处理**：发布前同时执行：

1. `npm run validate`
2. `npm run build`
3. `npm run qa:layout`
4. 按目标分辨率逐主题、逐页查看截图
5. 检查浏览器控制台、键盘翻页和鼠标翻页
6. 人工核对图片中的日期、账号、型号、商标和免责声明

## 模板仓库混入测试产品资料

**原因**：把真实项目内容直接当作模板默认数据提交，或只删除当前文件而没有检查 Git 历史。

**处理**：模板只保留占位文案和占位图片。提交前检查暂存区、仓库可见性、敏感路径和凭据；若私有素材已经进入公开历史，应停止发布并按最小范围清理历史，而不是只做一个删除提交。
